# Deep-packet-Inspection-System
A Java-based Deep Packet Inspection (DPI) engine that analyzes PCAP files, tracks network flows, extracts TLS SNI and HTTP Host information, identifies applications, and filters traffic using IP, domain, and application-based rules with both single-threaded and multi-threaded processing.
